<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        largeHeap: the engine loads ~8 MB of lexicons + voice data (main_lexicon.json 2.4 MB,
        stem_lexicon 0.9 MB, lt_lex.tsv 1.9 MB, gintaras.dta 3.8 MB) plus a large emoji-alternation
        regex into RAM. The live object graph needs >64 MB; on budget/older devices the default
        per-app heap (48–64 MB) is smaller than that, so loading threw OutOfMemoryError — an Error,
        NOT caught by the service's try/catch — crashing the process ("Gintaras TTS stopped") and
        leaving the engine silent. largeHeap raises the limit (device max, typically 128–512 MB).
    -->
    <application
        android:name=".GintarasApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/Theme.Gintaras"
        android:extractNativeLibs="true">

        <!--
            No LAUNCHER category → the app does not appear in the home-screen grid.
            MAIN is kept so the activity can still be invoked by the system (e.g. from
            the Apps list in Android Settings). It immediately forwards to SettingsActivity.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Settings screen — reachable via the gear icon in system TTS settings. -->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/general_settings"
            android:exported="true" />

        <!-- TTS service — the core synthesis engine (pure-Kotlin lt_tts port). -->
        <service
            android:name=".GintarasTtsService"
            android:label="@string/tts_engine_name"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.TTS_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.speech.tts"
                android:resource="@xml/tts_service" />
        </service>

        <!-- Reports bundled voices to the TTS framework (CHECK_TTS_DATA). -->
        <activity
            android:name=".CheckVoiceData"
            android:theme="@android:style/Theme.NoDisplay"
            android:exported="true">
            <intent-filter>
                <action android:name="android.speech.tts.engine.CHECK_TTS_DATA" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Returns sample text for the system TTS settings page (GET_SAMPLE_TEXT). -->
        <activity
            android:name=".GetSampleText"
            android:theme="@android:style/Theme.NoDisplay"
            android:exported="true">
            <intent-filter>
                <action android:name="android.speech.tts.engine.GET_SAMPLE_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>
